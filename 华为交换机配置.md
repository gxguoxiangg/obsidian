## 基础配置典型配置

### 交换机登录的典型配置

#### 配置通过Console口登录交换机

操作步骤：

```bash
# 配置交换机的基本信息
<HUAWEI> clock timezone Beijin add 08:00:00
<HUAWEI> clock date 10:10:00 2026-06-06
[HUAWEI] sysname Switch
```


```bash
[Switch] user-interface console 0
[Switch-ui-console0] authentication-mode aaa
[Switch] aaa
[Switch-aaa] local-user admin passwrod irreversible-cipher qazwsx123
[Switch-aaa] local-user admin privilege level 15
[Switch-aaa] local-user admin service-type terminal
```



#### 配置通过SSH登录交换机

操作步骤：

```bash
# 生成本地密钥对
dsa local-key-pair create

# 设置vty支持ssh
user-interface vty 0 14
	authentication-mode aaa
	protocol inbound ssh
	user privilege level 15

# 设置aaa中用户支持ssh
aaa
	local-user admin password irreversible-cipher admin@123
	local-user admin service-type ssh terminal
	local-user admin privilege level 15

# 创建ssh用户，需要和aaa中同名
ssh user admin authentication-type password
ssh user admin service-type stelnet

# 使能设备的STelnet服务器功能
stelnet server enable

# 允许通过任意接口ssh连接该设备
ssh server-source all-interface
```



### 文件管理典型配置

#### 通过SFTP进行文件操作示例

操作步骤：

```bash
# 使能 SFTP 服务器功能
sftp server enable
ssh server-source -i Vlanif 10

# 配置 ssh 用户
ssh user admin authentication-type password
ssh user admin service-type sftp
ssh user admin sftp-directory flash:

# 其余配置与登录配置相差无异
```



### 新设备升级示例

操作步骤：

#### 搭建FTP升级环境

1.通过Console和网线，将交换机与PC直连。

2.在PC上打开终端仿真软件，新建连接，设置连接的COM接口以及通信参数。设置终端软件的通信参数与设备的缺省值保持一致，分别为：传输速率为9600bit/s、8位数据位、1位停止位、无校验和无流控。

3.交换机和PC需要处于同一网段，可以ping通

版本升级操作：

```bash
# 10.10.1.2为终端PC的IP地址
<HUAWEI> ftp 10.10.1.2	
[ftp] get S5720-HI-V200R010C00SPC600.cc   # XXX.cc为系统软件的文件名。
[ftp] get S5720-HI-V200R010SPH013.pat     # XXX.pat为系统补丁的文件名。

# 检查内柔是否下载成功
<HUAWEI> dir flash:

# 设置交换机下次启动使用的系统软件和补丁
<HUAWEI> startup system-software S5720-HI-V200R010C00SPC600.cc 
<HUAWEI> startup patch S5720-HI-V200R010SPH013.pat

# 检查下次启动的启动项
<HUAWEI> display startup

# 重启设备
<HUAWEI> reboot fast
```



## 以太网接口典型配置

### 配置接口速率和双工模式示例

配置接口速率和双工模式可以在自协商或非自协商两种模式下进行：

- 在自协商模式下，接口速率和双工模式由链路两端的接口协商决定，一旦协商通过，链路两端的设备就锁定在同样的双工模式和接口速率。自协商功能只有在链路两端设备均支持时才可以生效。如果协商失败，则接口可能会处于Down状态。
- 当对端设备不支持自协商功能，或者配置自协商后设备无法连通、物理连通后接口出现大量错包或丢包，
- 户可配置本接口工作在非自协商模式下，手动配置接口速率和双工模式，调整接口的速率和双工模式。

![image.png](https://img.imgdd.com/45e6af11-2294-4bf6-a788-7e7ef6a12a29.png)

组网需求：

如图所示服务器群（Server1、Server2和Server3）分别与Switch的接口GE1/0/1、GE1/0/2和GE1/0/3相连，Switch通过接口GE1/0/4上行接入Internet网络。

由于服务器网卡的特殊限制，接口GE1/0/1、GE1/0/2和GE1/0/3只能自协商为半双工模式，在该双工模式下，当业务数据流量较大时将会产生丢包现象；同时，接口GE1/0/1、GE1/0/2和GE1/0/3速率自协商为最大速率1000Mbit/s，当服务器群同时以1000Mbit/s速率对外发送数据时，就会造成出接口GE1/0/4拥塞。用户希望解决数据丢包和拥塞问题。

配置思路：

- 配置接口工作在非自协商模式，避免服务器网卡影响设备接口的最终工作速率。
- 在非自协商模式下强制指定接口双工模式为全双工，避免发生数据丢包现象。
- 在非自协商模式下强制指定接口工作速率为100Mbit/s，避免发生数据拥塞现象。

操作步骤：

```bash
# 创建端口组，将 1/0/1、2、3 加入端口组中
[Switch] port-group portgroup1
[Switch-port-group-portgroup1] group-member G 1/0/1 to G 1/0/3
# 批量配置接口工作在非自协商模式，双工模式为全双工，工作速率为100Mbit/s
[Switch-port-group-portgroup1] undo negotiation auto
[Switch-port-gourp-portgroup1] duplex full
[Switch-port-group-portgroup1] speed 100
```

检验结果：

```bash
[Switch] display interface gigabitethernet 1/0/1
...
Port Mode: COMMON COPPER                                                        
Speed : 100,    Loopback: NONE                                                  
Duplex: FULL,   Negotiation: DISABLE                                            
Mdi   : AUTO,   Flow-control: DISABLE  
...
```



### 配置端口隔离示例

![image.png](https://img.imgdd.com/b639f30b-2fcd-4223-ba3d-87b8a7e02101.png)

组网需求：

某企业研发办公室员工分为本公司员工，A、B合作方员工，如图所示，公司希望在节省VLAN资源的前提下，实现本公司员工和A、B两个合作方公司之间可以相互通信，但是A、B两个合作方公司员工之间无法通信。

配置思路：

- 配置接口加入VLAN
- 设备缺省端口隔离为二层隔离三层互通，只需要将接口加入隔离组中，就可以实现隔离组内接口之间二层数据隔离。

操作步骤：

```bash
# 分别将1/0/1、2端口加入端口隔离组，属于同一组的相同之间不能通信
# 缺省模式为二层隔离三层互通
# 在系统视图下执行port-isolate mode all 可配置隔离模式为二三层都隔离
[Switch] int g 1/0/1
[Switch-G1/0/1] port-isolate enable

[Switch] int g 1/0/2
[Switch-G1/0/2] port-isolate enable
```



## 以太网交换典型配置

### MAC典型配置

#### 配置静态和黑洞MAC示例

手动配置静态MAC，一般是为了防止假冒身份的非法用户骗取数据，由网络管理员手动在MAC地址中添加合法用户的MAC地址表项。手动配置静态MAC地址表项的数量比较大时，不便于网络管理员维护，此时可以使用端口安全功能实现MAC地址和接口的动态绑定。

通过配置黑洞MAC，可以防止非法用户攻击，当交换机收到的报文源MAC或目的MAC是配置的黑洞MAC地址时，报文就会直接被丢弃。



操作步骤：

```bash
# 添加静态mac
[Switch] mac-address static xxxx-xxxx-xxxx4 g 1/0/2 vlan 2
# 添加黑洞mac
[Switch] mac-address blackhole xxxx-xxxx-xxx5 vlan 3
```

验证配置：

```bash
display mac-address static vlan 2
display mac-address blackhole
```



### 链路聚合典型配置

#### 配置手工模式链路聚合示例

链路聚合是指将多条以太网物理链路捆绑在一起成为一条逻辑链路，从而实现增加链路带宽的目的。链路聚合分为手工模式和LACP模式。手工模式下，所有活动链路都参与数据转发并分担流量。

操作步骤：

```bash
# 在 A、B交换机上都创建ID为1的Eth-Trunk接口组，将接口加入到接口组中
[SwitchA] int eth-trunk 1
[SwitchA-Eth-Trunk1] trunkport g 0/0/1 to 0/0/3

# 配置Eth-Trunk1 接口允许VLAN通过
[SwitchA] int eth-trunk 1
[SwitchA-Eth-Trunk1] port link-type trunk
[SwitchA-Eth-Trunk1] port trunk allow-pass vlan 10 20

# 配置 Eth-Trunk1 基于源MAC地址与目的MAC地址进行负载分担
[SwitchA] int eth-trunk 1
[SwitchA-Eth-Trunk1] load-balance src-dst-mac

# SwitchB 需要进行一样的配置
```

验证配置：

```bash
[SwitchA] display eth-trunk 1
Eth-Trunk1's state information is: 
WorkingMode: NORMAL           Hash arithmetic: According to SA-XOR-DA
Least Active-linknumber: 1     Max Bandwidth-affected-linknumber: 8
Operate status: up             Number Of Up Port In Trunk: 3 
--------------------------------------------------------------------------------
PortName                           Status       Weight
GigabitEthernet0/0/1               Up           1
GigabitEthernet0/0/2               Up           1
GigabitEthernet0/0/3               Up           1
```



####	配置LACP模式的链路聚合示例

LACP模式可以提高Eth-Trunk的容错性、提供备份功能。LACP模式下，部分链路是活动链路，所有活动链路均参与数据转发。如果某条活动链路故障，链路聚合组自动在非活动链路中选择一条链路作为活动链路，参与数据转发的链路数目不变。

![image.png](https://img.imgdd.com/578b2c15-8a90-4283-a0ac-2a934ef471ae.png)

操作步骤：

```bash
# 创建Eth-Trunk1并配置LACP模式
[SwitchA] interface eth-trunk 1
[SwitchA-Eth-Trunk1] mode lacp

# 将成员接口加入Eth-Trunk1
[SwitchA-Eth-Trunk1] trunkport g 0/0/1 to 0/0/3

# 在SWA上配置系统优先级为100，使其成为LACP主动端
[SwitchA] lacp priority 100

# 在SWA上配置活动接口上限阈值为2
[SwitchA-Eth-Trunk1] max active-linknumer 2

# 配置接口优先级确定活动链路
[SwitchA] int g 0/0/1
[SwitchA-G0/0/1] lacp priority 100
[SWitchA] int g 0/0/2
[SwitchA-G0/0/2] lacp priority 100

# 配置Eth-Trunk1接口允许VLAN10和VLAN20通过
[SwitchA] interface eth-trunk 1
[SwitchA-Eth-Trunk1] port link-type trunk
[SwitchA-Eth-Trunk1] port trunk allow-pass vlan 10 20

# SwitchB 需要需要执行相同的操作
```

验证配置：

```bash
[SwitchA] display eth-trunk 1
Eth-Trunk1's state information is:
Local:                                                                          
LAG ID: 1                       WorkingMode: LACP                             
Preempt Delay: Disabled         Hash arithmetic: According to SIP-XOR-DIP       
System Priority: 100            System ID: 00e0-fca8-0417
Least Active-linknumber: 1      Max Active-linknumber: 2                        
Operate status: up              Number Of Up Port In Trunk: 2
--------------------------------------------------------------------------------
ActorPortName                    Status     PortType PortPri   PortNo PortKey   PortState  Weight
GigabitEthernet0/0/1             Selected  1GE       100      6145    2865      11111100     1
GigabitEthernet0/0/2             Selected  1GE       100      6146    2865      11111100     1
GigabitEthernet0/0/3             Unselect  1GE       32768    6147    2865      11100000     1

Partner:
--------------------------------------------------------------------------------
ActorPortName                     SysPri    SystemID    PortPri PortNo PortKey   PortState
GigabitEthernet0/0/1              32768  00e0-fca6-7f85  32768     6145   2609      11111100
GigabitEthernet0/0/2              32768  00e0-fca6-7f85  32768     6146   2609      11111100
GigabitEthernet0/0/3              32768  00e0-fca6-7f85  32768     6147   2609      11110000
```





### VLAN典型配置



#### 配置不同网段之间通过静态路由进行通信示例

![image.png](https://img.imgdd.com/a8a69d47-4c7e-4f3f-8a12-bddb8f268896.png)

组网需求：

要求用户与服务器之间互通。



操作步骤：

```bash
[ACC1]
vlan 10
int g 1/0/1
	port link-type access
	port default vlan 10
int g 1/0/2
	port link-type trunk
	port trunk allow-pass vlan 10

[AGG]
vlan 10 30
int g 1/0/2
	port link-type trunk
	port trunk allow-pass vlan 10
int g 1/0/3
	port link-type trunk
	port trunk allow-pass vlan 30
int vlanif 30
	ip add 10.10.30.1 24
	
ip route static 192.168.1.0 24 10.1.1.2

[CORE]
vlan 20 30
int g 1/0/3
	port link-type trunk
	port trunk allow-pass vlan 10 30

int vlanif 30
	ip add 10.10.30.2 24
int vlanif 20
	ip add 192.168.1.1 24

int g 1/0/2
	port link-type trunk
	port trunk allo-pass vlan 20

ip route static 10.1.1.0 24 10.10.30.1
	
```



#### 配置Super VLAN示例

Super VLAN（VLAN 聚合）的主要功能就是节约IP地址，隔离广播风暴。Super VLAN 下关联多个 Sub VLAN，Sub VLAN之间二层隔离，所有 Sub VLAN 共用 Super VLAN 的 VLANIF 接口IP地址与外界通信，并可以通过该接口三层互通，从而节约IP地址。

![image.png](https://img.imgdd.com/13de1ed6-29d2-4cd1-bdcf-f96a6022082d.png)

组网需求：

某公司拥有多个部门且位于同一网段，为了提升业务安全性，将不同部门的用户划分到不同VLAN中，如图所示，VLAN2或VLAN3属于不同部门，各部门均有访问Internet需求，同时由于业务需要，不同部门间的用户需要互通。

操作步骤：

```bash
# 配置 Super-VLAN 4 将 VLAN2、3 加入到 Super-VLAN4
[SWB] vlan 4
[SWB-vlan4] aggregate-vlan
[SWB-vlan4] access-vlan 2 to 3

# 在SwitchB的Super-VLAN 4下配置Proxy ARP，使不同部门的用户间三层互通。
[SWB] interface vlanif 4
[SWB-vlan4] arp-proxy inter-sub-vlan-proxy enable
```

配置VLAN聚合时，同一个Super-VLAN内的主机使用的是同一个网段的地址并共用一个网关地址，即使是属于不同Sub-VLAN的主机，由于他们同属于一个子网，彼此通信时只会查ARP表项进行转发，而不会通过网关转发，使用Proxy ARP可以使得所有Sub-VLAN建立相应的ARP表项进行互通。

如果只让某些Sub-VLAN之间互通，可以通过在主机上配置静态ARP来实现，将访问目的主机的IP地址与网关MAC地址绑定。



### MSTP典型配置

#### 配置MSTP功能示例

![image.png](https://img.imgdd.com/8e005531-1965-4df3-b2b9-83a5f7440579.png)

配置思路：

1. 在处于环形网络中的设备上配置MSTP基本功能，与PC相连的端口不用参与MSTP计算，将其设置为边缘端口。
2. 配置保护功能，实现对设备或链路的保护，比如在根桥的指定端口配置根保护功能。

操作步骤：

```bash
# 配置MST域，使得设备都在一个域中，并配置一样的vlan映射表
[SWA] stp region-configuration
[SWA-mst-region] region-name RG1
[SWA-mst-region] instance 1 vlan 2 to 10
[SWA-mst-region] instance 2 vlan 11 to 20
[SWA-mst-region] active region-configuration

# 配置 MSTI1 和 MSTI2 的根桥和备份根桥
[SWA] stp instance 1 root primary
[SWA] stp instance 2 root secondary

# 配置实例中MSTI1和MSTI2中将要被阻塞的路径开销值大于缺省值
[SWC] stp pathcost-standard legacy
[SWC] int g 0/0/2
[SWC] stp instance 2 cost 20000

# 将与终端相连的端口设置为边缘端口
[SWC] int g 0/0/1
[SWC-G0/0/1] stp edged-port enable
[SWC] stp bpdu-protection

# 配置保护功能，如在各实例的根桥设备的指定端口配置根保护功能。
[SWA] int g 0/0/1
[SWA-G0/0/1] stp root-protection
```

